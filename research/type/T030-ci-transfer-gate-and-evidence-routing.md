# T030 — CI Transfer Gate and Evidence Routing

Evidence purpose: **TRANSFER VALIDATION / GATE PROTECTION**

## RELATED DOMAIN CHECK
W061 identifies GitHub Actions as an available execution substrate. C060, L051/I047 and CD066 share the same MintTap scenario identity. T021 drawing remains the governing Type gate.

## Finding
The next MintTap runtime evidence must use mature fallback while T021 R1 drawing is open. CI execution does not authorize custom-font promotion and does not change the drawing → general spacing → residual kerning sequence.

## CI Type capture contract
For every W061 scenario capture:
- exact product commit/build/scenario identity;
- actual font family/fallback where inspectable;
- viewport and text-scaling condition;
- full truth-bearing string;
- line count/wrap/clipping/overflow result;
- screenshot/raster artifact;
- failure class from T027/T029: glyph/drawing, metrics, general spacing, pair residual, fallback, composition pressure, semantic truncation or raster.

The compact-iOS 1.10 cap and unclamped diagnostic must be recorded as separate conditions. A difference between them is composition/accessibility evidence until a genuine glyph/metric defect is demonstrated.

## Gate rule
1. unfinished drawing is never repaired with kerning;
2. product layout is not frozen around provisional custom metrics;
3. necessary financial/localized wording is not shortened to preserve preferred geometry;
4. runtime wrap/clipping pressure routes to Layout/Web unless Type evidence demonstrates a Type defect;
5. T021 remains OPEN until its own bounded mutation/raster evidence passes.

## Next block
Consume the first W061 artifact manifest. Independently classify all typography-related failures, then resume T021 bounded R1 mutation in its own normalized toolchain.

## HANDOFFS
Layout receives composition pressure; Content receives semantic truncation; Web receives browser/runtime differences; Color receives text/background rendering contexts.

## Evidence boundary
No custom-font production recommendation, T021 closure, kerning entry, native/browser breadth, AT or human readability PASS is claimed.