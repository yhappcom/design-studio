# T051 — Semantic-Identity Status Transfer Corpus

## Purpose
Extend T050 with strings produced by I069/CD088 semantic-object binding while preserving the T021 gate. This is transfer corpus preparation, not drawing/spacing closure.

## RELATED DOMAIN CHECK
I069/L073, C082, W081 and CD088 checked. Recomposition/status pressure is not evidence that Type should change provisional glyph metrics.

## Protected corpus
Include full/compact aviation identifiers and repeated mutation/status combinations: `DEP`, `ARR`, `PIC`, `SIC/FO`, `PICUS`, `SPIC`, `PF/PM`, `T/O`, `L/D`, `Instrument Flight Time`, `Simulator / FSTD`, positions `1/35`, `9/35`, `10/35`, `19/35`, `20/35`, `34/35`, `35/35`, plus concise and expanded move/hide/Reset/Undo status realizations.

## Gate discipline
T021 remains authoritative: bounded R1 drawing first with widths/sidebearings frozen and kerning OFF; then general spacing; only residual pair-specific defects may enter kerning. T051 must use mature fallback for product transfer while R1 remains open.

## Future validation
After T021 permits: render exact strings at operational sizes and 200% conditions; capture family/style/weight, fallback, locale, line count/wrap/clipping and raster evidence. Attribute failure to Type only when glyph/metric/fallback/raster evidence reproduces it; otherwise return composition pressure to Layout/Content.

## OPEN
No repaired-candidate T051 execution, custom-font browser/native transfer, multilingual raster, AT or human recognition evidence.

## HANDOFFS TO OTHER SPECIALISTS
Content owns wording; Interaction owns identity; Layout owns fit/recomposition; Color owns state encoding; Web owns browser transfer. Type will not shorten or kern away semantic requirements.