# I012 — W024 six-state action-invariant transfer

Classification: **TRANSFER VALIDATION + CONTRADICTION PREVENTION**

## Purpose
Extend I011's unknown→confirmed invariant to the complete canonical certainty/recovery set.

## RELATED DOMAIN CHECK
Type is not state authority; Color C026 reinforces but does not define states; Layout L017 confirms spatial survival; Web W024 executes the fixture; Content CD031 verbalizes the contract; UX comprehension remains human evidence.

## Executed matrix
- pending → no competing action;
- confirmed → Edit record;
- known failure → Try again;
- outcome unknown → Check record;
- offline/stale → View local record;
- conflict → Compare versions.

The rendered DOM preserved a different authorized action contract for each state at desktop and narrow widths.

## Invariant review
PASS, bounded:
1. failure permits retry because non-save is known;
2. unknown blocks blind retry and requires verification;
3. conflict requires comparison rather than silent overwrite;
4. offline/stale exposes the local object rather than claiming sync success/failure beyond known truth;
5. pending does not expose a competing mutation action;
6. confirmed permits ordinary record work.

These are deterministic fixture semantics. No backend/network truth was exercised, so the fixture does not prove that a production system classifies states correctly.

## UX boundary
The state/action model is internally coherent, but discoverability, action-choice accuracy, cognitive load, trust and interruption recovery require later human/product evidence.

## Handoff
Content must preserve these distinctions across locale/surface realization. Color must not merge them into generic warning hues. Web must bind future network outcomes to these semantic states rather than derive state from copy. Layout must preserve recovery visibility under overlays/keyboards.

## Verdict
**I012 six-state action-invariant transfer PASS for the deterministic Chromium fixture; Interaction Stage 3 remains PRACTICE.**