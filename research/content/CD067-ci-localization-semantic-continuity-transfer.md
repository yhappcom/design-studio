# CD067 — CI Localization Semantic-Continuity Transfer

Evidence purpose: **TRANSFER VALIDATION**

## RELATED DOMAIN CHECK
W061 makes the actual MintTap Dart localization pipeline executable in CI; T030 handles fit/rendering pressure; C061 reinforces state; L052 preserves semantic adjacency; I048 owns state/action/recovery truth.

## Objective
Materialize CD065/CD066 through the product's current Dart localization implementation rather than assuming an ARB/TMS prerequisite.

## CI capture ledger
For each selected scenario record, as available:
- source key/API and typed variables;
- locale and currency;
- visible realization;
- accessible/semantic realization obtainable from the harness;
- fallback behavior;
- history/export realization when present;
- product state and recovery action that selected the message.

## Mandatory semantic contrasts
- portfolio value versus invested capital;
- total performance versus distributions;
- gross versus net;
- estimated versus final ROC;
- partial/unavailable versus zero;
- tax adjustment direction;
- pending/failure/ambiguous outcome;
- KRW/USD and long localized labels.

## Failure classes
`SURFACE_DRIFT`, `LOCALE_STATE_COLLISION`, `ACCESSIBLE_NAME_LOSS`, `HISTORY_EXPORT_DRIFT`, `ESTIMATE_FINAL_COLLAPSE`, `PARTIAL_ZERO_COLLAPSE`, `GROSS_NET_COLLAPSE`, `OUTCOME_CERTAINTY_COLLAPSE`.

## Writing boundary
If a truth-bearing string creates fit pressure, route it to T030/L052/W061. Do not delete qualifiers merely to satisfy preferred geometry. If the underlying state/action contract is ambiguous, route it to I048 rather than inventing reassuring copy.

## Next block
Execute the corpus in the first W061 artifact-bearing run and compare source → locale → visible/semantic → recovery/history continuity. Linguistic review and representative-human comprehension remain OPEN.

## HANDOFFS
Type receives rendering pressure, Color receives explicit state semantics, Layout receives information-order requirements, Interaction receives ambiguous state/action dependencies, Web packages runtime evidence.

## Evidence boundary
No Stage 3 PASS, full locale runtime PASS, linguistic review, AT comprehension or human task PASS is claimed.