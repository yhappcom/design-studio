# CD073 — Repair Semantic Invariants + Localization Entry

Evidence class: **SYSTEMS PRACTICE / TRANSFER VALIDATION PLAN**

## PURPOSE
Prevent L057 repair from converting fit pressure into semantic loss, while defining the next executable entry into MintTap's real Dart localization pipeline.

## SEMANTIC INVARIANTS
The repair must preserve distinctions already established by the content system: distribution ≠ profit; partial/unavailable ≠ zero; estimated ≠ final; gross ≠ net; ambiguous outcome ≠ known success/failure. Qualifiers that carry one of these distinctions are protected content, not decorative overflow budget.

## PRACTICE — repair review
For every string changed during compact recomposition, classify the change as: unchanged; line-break/reflow only; abbreviation with equivalent meaning; semantic deletion; terminology substitution; or state/consequence change. Only the first three can proceed without a product-truth review, and abbreviation still requires localization/translatability review.

## LOCALIZATION ENTRY
After smoke repair, execute actual MintTap Dart locale keys/typed variables/fallback behavior rather than substituting an assumed ARB/TMS pipeline. Compare at least source English plus the product's high-risk long/financial locale fixtures. Record key identity, variable interpolation, fallback, visible string, accessibility string where available, and history/export realization.

## CRITIQUE
Shorter is not inherently clearer when it deletes financial qualifiers. Conversely, preserving every source-language word in one rigid row is not semantic fidelity; correct responsive sequencing may move a qualifier while preserving its relationship to the value/state.

## RELATED DOMAIN CHECK
T036 protects Type from wording-as-fit-fix. C067 requires explicit non-color semantics. L058 owns recomposition and adjacency. I054 owns state/action feedback. W067 owns execution identity and browser/runtime progression.

## HANDOFFS TO OTHER SPECIALISTS
Layout receives protected semantic groups rather than fixed strings-as-geometry. Type receives actual locale strings after the real pipeline executes. Web receives locale/runtime discrepancy requirements. Interaction receives any wording change that would alter action, consequence or recovery truth.

## OPEN
The real MintTap locale pipeline remains unexecuted. Linguistic review, AT comprehension and representative-human task evidence remain OPEN.