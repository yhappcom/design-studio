# CD123 — Candidate 05 semantic/accessibility transfer

Status: **STAGE 3 PRACTICE / TRANSFER VALIDATION / RUNTIME OPEN**

## Question
Can Candidate 05 preserve visible wording, accessible names/roles/states and implemented action truth through scaling/reflow and runtime-state injection?

## RELATED DOMAIN CHECK
T086, C117, I104, L108, W116 and CD122 checked. Candidate 05 has static owner-review approval only; Search/runtime status/accessibility behavior remain open.

## Semantic contract
`product truth → semantic message contract → locale realization → surface realization → render/runtime realization` remains authoritative.

Candidate 05 visible English strings are immutable-first. Geometry failure does not authorize abbreviation. Icon-only month controls require intelligible accessible names tied to the real action. Visible labels, accessible names/roles/states and destinations must describe the same function.

## PRACTICE
From the exact runtime build capture, together:
- visible label;
- semantic identifier;
- accessible name/role/state;
- action/destination ID;
- state message if any;
- rendered line/wrap state.

Repeat after maximum text scaling, forced mature fallback and adaptive reflow. Inject error/pending/offline/recovery copy only when I104 exposes the corresponding state. Search remains unlabeled as a functioning retrieval system until SEARCH-001 is implemented.

## CRITIQUE / failure conditions
- FAIL if copy promises Search, Saved, Synced, Offline or recovery behavior not implemented.
- FAIL if an icon-only action lacks an intelligible accessible name.
- FAIL if visible wording and accessibility payload imply different destinations/consequences.
- FAIL if strings are shortened merely to preserve the owner-review screenshot.
- FAIL if pending, failure and unknown outcome are collapsed for brevity.

## Human boundary
Expert/static review, semantic-tree inspection and automated checks do not establish comprehension, trust, preference or representative-pilot task performance.

## HANDOFFS TO OTHER SPECIALISTS
I104 is authority for action/state truth. T086/L108 must first attempt rendering/recomposition before Content changes wording. C117 must not rely on color-only references. W117 should capture visible and semantic payload from the same build.