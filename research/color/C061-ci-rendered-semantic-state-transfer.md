# C061 — CI Rendered Semantic-State Transfer

Evidence purpose: **TRANSFER VALIDATION**

## RELATED DOMAIN CHECK
W061 provides the shared execution substrate; T030 protects typography gate order; L051/I047 preserve spatial/behavioral state; CD066 owns explicit financial/localized meaning.

## Objective
Turn C060's source preflight into a finite rendered CI test without treating automation as observer/human evidence.

## Required state crossings
The first W061 artifact run must include:
- ko/ja positive-red / negative-blue mapping and non-ko/ja positive-green / negative-red mapping;
- neutral zero;
- high distribution + negative total performance contradiction;
- estimated versus final;
- partial/unavailable versus zero;
- gross versus net;
- positive/negative tax adjustment where product truth supports it.

## Pass conditions
For every state:
1. explicit sign/label/semantic wording survives without hue;
2. brand mint is not substituted for generic financial success;
3. outcome polarity, certainty/finality and availability remain independent axes;
4. applicable WCAG 2.2 rendered contrast checks are recorded separately from grayscale/color-loss/forced-colors diagnostics;
5. locale-dependent conventional colors never invert the underlying numeric or linguistic truth.

## Failure routing
`COLOR_ONLY`, `AXIS_COLLISION`, `BRAND_AS_SUCCESS`, `AVAILABILITY_AS_ZERO`, contrast failure or locale-polarity mismatch remains a Color failure. Missing/incorrect state truth routes to Interaction/Content rather than being patched by palette choice.

## Next block
Consume the first W061 CI artifact manifest and perform a contradiction review across all required state crossings. Keep calibrated-display, observer and representative-human evidence OPEN.

## HANDOFFS
Content supplies explicit state names; Layout preserves label/value/qualifier adjacency; Type preserves signs and numerals; Web records actual browser/theme artifacts.

## Evidence boundary
No Stage 3 PASS, physical-device, calibrated-display, observer or human PASS is claimed.