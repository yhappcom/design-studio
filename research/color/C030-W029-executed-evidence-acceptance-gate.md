# C030 — W029 executed evidence acceptance gate

Evidence type: **SYSTEMS PRACTICE / CRITIQUE GATE / EXECUTION OPEN**

## RELATED DOMAIN CHECK
W029/W030 own browser execution and environment truth; L020 owns rectangles/reflow; I016 owns recovery classification; CD035 owns semantic language; Type T021 remains separate.

## Acceptance rule
Color Stage 3 evidence may consume a W029 capture only when the capture identifies an actually executed browser engine/environment. A runner, screenshot mock, CSS inspection or 320px simulation alone is insufficient.

For each accepted capture, C029/C030 records independently:
- semantic state is named without hue;
- authored text/background contrast evidence where measurable;
- non-text boundary evidence where applicable;
- focused component and focus-indicator visibility;
- authored overlay intersection and whether focus is entirely obscured;
- theme;
- forced-colors/high-contrast mode as a separate executed transfer;
- browser/platform metadata.

## Failure discipline
Do not repair semantic ambiguity with color. Do not use contrast PASS to waive focus obscuration. Do not use forced-colors survival to infer ordinary-theme contrast. A 320 CSS px run is responsive evidence, not 200% zoom evidence.

## Current result
**ACCEPTANCE GATE READY / EXECUTED COLOR TRANSFER OPEN.** W030 records that the current container could not obtain the repository because external DNS/network access was unavailable, so no C030 runtime PASS is claimed.

## HANDOFFS
Web supplies executed capture artifacts; Layout supplies geometry interpretation; Content verifies non-color state naming; UX integration checks workflow coherence while human salience remains OPEN.