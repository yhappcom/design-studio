# W027 — True-Origin Runtime Execution Harness

Evidence type: PRACTICE / TRANSFER VALIDATION / OPEN

## RELATED DOMAIN CHECK
T023 supplies mature-font/product-string transfer constraints. C028 requires focus/occlusion and forced-colors observations. L019 requires viewport/focus/overlay geometry. I014 owns outcome-unknown and reconciliation semantics. CD033 supplies stable resource/message IDs. W026 supplies the joined evidence record.

## Executable advance
`W027-true-origin-execution-harness.html` is a concrete route/history/Fetch specimen rather than another static state screenshot. It contains browser history traversal, POST dispatch, transport-failure classification, explicit outcome-unknown wording, a separate reconciliation action, a sticky authored layer for focus-obscuration checks, and a long-string geometry stressor.

The specimen intentionally requires an HTTP origin and backend endpoints `/w027/operation` and `/w027/status`; opening the file alone cannot satisfy the study. This prevents a local-file fixture from being mislabeled true-origin evidence.

## Required execution matrix
For each W026 capture ID: load/reload/deep-link; back/forward traversal; confirmed POST; known HTTP failure; connection abort before response; response loss after possible commit; reconciliation; 200% zoom; narrow viewport; keyboard traversal across the sticky layer; forced-colors where supported; localized expansion. Repeat the coherent workflow in Chromium, Firefox and Safari/WebKit when executable.

## Accessibility baseline
WCAG 2.2 remains the studio baseline. Focus-obscuration evidence must distinguish SC 2.4.11 Minimum (AA) from the stronger AAA criteria; color contrast alone cannot establish non-obscuration.

## Performance boundary
This harness may produce lab diagnostics only. It does not create field LCP/INP/CLS evidence. Field Core Web Vitals require actual field/RUM population context and remain OPEN.

## Current result
**HARNESS AUTHORED / TRUE-ORIGIN EXECUTION OPEN.** The repository now has an executable specimen, but this connector environment has not served it from an HTTP origin or executed real browser/backend/network interruption. No cross-browser, AT, physical-device or field-performance PASS is claimed.

## HANDOFFS TO OTHER SPECIALISTS
I014 receives actual operation/outcome evidence when executed. L019/C028 consume the same capture IDs for geometry/focus. CD033 binds resource IDs/locales. T023 measures mature-font behavior on the same rendered strings. UX integration evaluates the resulting end-to-end recovery path without treating non-human inspection as usability evidence.