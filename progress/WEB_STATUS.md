# Web Design Specialist Status

Operating state: **ACTIVE — STAGE 3 PRACTICE / W033 PRODUCT-LIKE RESUMPTION TARGET READY — RUNTIME OPEN**
Governance sync: 2026-09-16
Primary path: `research/web/`
Active studies: `W029`–`W033`

## Current level
Stage 1 **PASS**; Stage 2 **PASS**; Stage 3 **PRACTICE / NOT PASSED**.

## Latest evidence
W033 advances beyond portable execution plumbing to a product-like resumption fixture contract. Required scenarios now preserve stable object/operation IDs, deep-link/route, authoritative persisted outcome, reconciliation, history/detail and conflict variants across response loss, reload, navigation away/back and later return. The shared provenance schema includes engine/OS, viewport + visualViewport, actual zoom method/value, forced-colors, locale, semantic state/action and geometry.

The browser execution blocker remains: no executed multi-engine W033 artifact exists. Safari-specific claims still require Safari execution. WCAG 2.2 remains the baseline. Functional/lab timings remain diagnostics; field LCP/INP/CLS require actual field/RUM population context.

## Active queue
1. Implement/execute W033 in a browser-capable CI/local environment and preserve raw artifacts with commit SHA.
2. Require Chromium plus an independent engine before cross-browser claims; execute Safari separately for Safari claims.
3. Bind actual 200% zoom, forced-colors, I020/L024 resumption, C033 degradation and CD039 localization/content to shared run IDs.
4. Add AT/physical mobile only when executable.
5. Preserve field-vs-lab performance boundary.

## Cross-domain state
C033 owns cue degradation; L024/I020 own resumption structure/action truth; CD039 owns retrieval/resumption content; custom Type remains behind T021.

## Evidence boundary
No Web Stage 3 PASS, browser-runtime PASS, cross-browser, Safari, screen-reader, physical-device, field Core Web Vitals or human UX PASS is claimed.