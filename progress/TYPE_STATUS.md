# Typography / Type Design Specialist Status

Operating state: **ACTIVE — STAGE 2 PRACTICE / LOGMATE TYPE IDENTITY PRIORITY / T021 ARCHITECTURE CONTRACT RECONCILED, SUCCESSOR IMPLEMENTATION NEXT**  
Governance sync: 2026-09-16  
Primary path: `research/type/`  
Next new-study ID: `T022` only after T021 drawing and spacing stabilize

## Current level
Stage 1 — Foundations: **PASS**  
Stage 2 — Intermediate Professional Practice: **PRACTICE / NOT PASSED**

Authority: T019 Stage 1; T020 Stage 2 entry; T021 ongoing operational-family authorship.

## Latest evidence
`T021-architecture-contract-reconciliation.md` found a material contradiction between the executable reset dictionary and the canonical consumer matrix. The prior CI perturbation result (5 FULL / 2 PARTIAL / 4 NONE) remains valid evidence about the old executable dictionary, but it is no longer a valid completion target.

The executable dictionary contains `corner` even though the consumer matrix gives it no canonical semantic obligation, while the matrix requires `shoulder_tension` but the executable architecture has no such token. Generic `join` / `terminal` names also underspecify the intended `bowl_join` / `terminal_policy` semantics. Forcing obsolete or non-homologous consumers to change merely to turn the old audit green is rejected.

Normalized successor vocabulary: `stem`, `round_stem`, `cap_overshoot`, `x_overshoot`, `aperture`, `diag_comp`, `terminal_policy`, `bowl_join`, `shoulder_tension`, `figure_width_mode`, `zero_treatment`. `corner` is removed unless later evidence demonstrates an independent reusable corner system.

The sensitivity method is retained but its semantics change: mandatory consumers must all show the intended delta; conditional/homologous consumers are tested only when the construction actually shares that parameter; protected unrelated glyphs must remain invariant where specified.

## Evidence chain
`repertoire → semantic architecture contract → construction provenance → mandatory/conditional/protected consumer assertions → executable sensitivity → operational completeness → 36/36 build → 14/17/24 raster → drawing validity → general spacing → pair residual → kerning`

## Decision boundary
Do not patch the old 5/2/4 score to 11/11. Build a successor harness with normalized architecture semantics, then run a successor semantic-sensitivity audit. Only semantic FULL reopens A/B raster comparison. If fully operational A/B still fail drawing, execute bespoke-vs-current-Roboto comparison rather than another serial patch cycle.

## Stage 2 snapshot
- coherent glyph family: **FAIL / PRACTICE**
- bounded repertoire: **36/36 prior exact proof**
- construction provenance: **PASS for prior complete harness**
- old executable token sensitivity: **MEASURED — 5 FULL / 2 PARTIAL / 4 NONE**
- architecture/consumer vocabulary consistency: **RECONCILED AS SPECIFICATION; successor executable OPEN**
- executable architecture operationality: **FAIL / REWORK**
- drawing: **FAIL on current A/B**
- spacing: **BLOCKED by drawing**
- kerning/T022: **BLOCKED**

## Active queue — large block only
1. create successor normalized architecture harness without mutating historical evidence;
2. implement `shoulder_tension` in n and only defensibly homologous transitions;
3. implement `bowl_join` across B/D/P/R/8;
4. implement `diag_comp` across actual diagonal structures;
5. implement `terminal_policy` in actual open-form/straight terminal structures;
6. define aperture mandatory consumers separately from conditional S/5 hypotheses;
7. revise CI sensitivity audit to mandatory/conditional/protected semantics;
8. obtain semantic FULL before reraster;
9. then rebuild 36/36 A/B, reraster 14/17/24, critique drawing;
10. if both fail, compare bespoke route against current proportional Roboto;
11. open spacing only after drawing PASS; T022 only after spacing stabilizes.

## HANDOFFS
- Layout/Web continue using flexible/current product geometry, not A/B metrics.
- Content preserves literal identifiers/numerics.
- Color does not compensate ambiguity.

## Evidence boundary
No successor executable operationality PASS, custom-family drawing PASS, human readability/recognition, product preference, native/browser or physical-device PASS is claimed.
