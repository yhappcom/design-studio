# Typography / Type Design Specialist Status

Operating state: **ACTIVE — STAGE 2 PRACTICE / T021 CLOSED / T022 EXACT PRODUCT ARTIFACT GATE OPEN**  
Governance sync: 2026-09-16  
Primary path: `research/type/`  
Active study: `T022`

## Current level
Stage 1 **PASS**; Stage 2 **PRACTICE / NOT PASSED**.

Authority: T019 Stage 1; T020 Stage 2 entry; T021 operational-family authorship/route decision; **T022 mature product typography role system**.

## Current route
T021 is closed: full bespoke LogMate family repair stopped after normalized architecture/build success but A/B drawing failure and product-route comparison. Current control remains exact bundled proportional `LogMateRoboto` 400/500 with `LogMateNotoSansKR` 400 companion.

T022 now compares P = proportional current behavior, R = role-specific tabular behavior for comparison-critical numeric/time/total roles if exact product + native evidence supports it, and M = bounded mono/identifier treatment only as a later exception. Global monospace is not assumed.

## Latest evidence
`T022-role-system-transfer-and-gate-review.md` strengthens selection criteria and cross-domain ownership without fabricating unavailable binary results. Numeric alignment is joint Type/Layout; airport/identifier strings keep natural widths; operational literals remain typed Content data; browser feature behavior is not Flutter proof; hue never repairs ambiguity.

The exact product-binary gate remains OPEN: relevant GSUB/GPOS inventory, digit/punctuation advances, P/R operational-string widths and feature isolation must be measured on the bundled artifact. Flutter/native feature activation must then be verified separately. Mixed Korean/Latin fallback is a distinct rendered/native seam and is not inferred from Latin metrics.

## Gate sequence
`exact product artifact → role corpus → default metrics → feature availability → exact P/R behavior → layout consequence → role-system selection → native transfer → residual kerning only if needed → human validation later`

## Active queue
1. obtain/inspect exact bundled `Roboto-Regular.ttf` artifact and measure P/R role corpus;
2. record digit and punctuation advances plus equal-length time/total consequences;
3. determine whether feature activation affects only intended roles;
4. transfer selected hypothesis to Flutter/native before recommendation;
5. separately inspect LogMateRoboto/LogMateNotoSansKR mixed-script weight/metric seam;
6. open kerning only for residual selected-role problems, never as numeric-alignment repair.

## HANDOFFS
L014 waits for exact numeric metrics before column freeze; CD024 preserves typed operational literals; W021 mature-font browser behavior is comparative only; C023 keeps ambiguity non-color.

## Evidence boundary
No exact font-binary metric/OpenType feature, Flutter activation, mixed-script rendered, human recognition or physical-device PASS is currently claimed. Stage 2 remains PRACTICE.