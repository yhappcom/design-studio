# Layout, Spatial & Interaction Specialist Status

Operating state: **ACTIVE — STAGE 3 PRACTICE / I018 END-TO-END ORACLE + L022 REFLOW CONTRACT READY**
Governance sync: 2026-09-16
Canonical paths: `research/layout/`, `research/interaction/`
Active studies: L020–L022, I016–I018

## Current level
Stage 1 **PASS**; Stage 2 **PASS**; Stage 3 **PRACTICE / NOT PASSED**.

## Latest evidence
I018 expands the tested ambiguity/reconciliation invariant into a complete professional-record workflow: retrieval → consequential operation → pending/ambiguity → safe action → reconciliation → conflict if required → stable history/retrieval. It adds invariants for no invented certainty, no unsafe retry promotion, history coherence, interruption continuity and retrieval continuity.

L022 defines the corresponding geometry/reachability evidence across narrow reflow, actual 200% zoom, localization, sticky layers and keyboard-reduced viewport while keeping each transfer class distinct. These are contracts until executed browser/product captures exist.

## Active queue
1. Execute I018/L022 against a stable W031 browser/product fixture with shared capture IDs.
2. Obtain documented production idempotency/deduplication and persistence contracts before retry-safe claims.
3. Execute actual zoom, focus/overlay and keyboard-reduced viewport geometry where supported.
4. Add native Flutter transfer only on executable app surface.
5. Keep AT, physical-device, discoverability, workload and human task evidence OPEN.

## HANDOFFS
C031 consumes same-capture state/overlap evidence; W031 owns browser integration; CD037 maps semantic IDs across the workflow; Type supplies custom metrics only after T021.

## Evidence boundary
No Layout/Interaction Stage 3 PASS, browser geometry PASS, production-backend PASS, AT, physical-device or human PASS is claimed.